# Build output
/build/
/out/

# CMake artifacts
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CMakeUserPresets.json
install_manifest.txt

# MSVC build output
x64/

# nlohmann_json fetched from git at build time (not committed)
/RMAP/third_party/json/

# asio fetched from git at build time (not committed)
/RMAP/third_party/asio/

# websocketpp fetched from git at build time (not committed)
/RMAP/third_party/websocketpp/

# BoringSSL fetched + built from git at build time (not committed)
/RMAP/third_party/boringssl/

# libcurl fetched + built from git at build time (not committed)
/RMAP/third_party/curl/

# socket.io-client-cpp fetched + built from git at build time (not committed)
/RMAP/third_party/socketio/

# IDE / editor
*.user
.vs/
.vscode/
